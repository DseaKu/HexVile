#ifndef ENUMS_H
#define ENUMS_H

enum class TileID {
  TILE_NULL = 0,
  TILE_VOID,
  TILE_GRASS,
  TILE_WATER,
};

enum class PlayerAnimationID {
  PLAYER_ANIMATION_NULL = 0,
  PLAYER_ANIMATION_IDLE,

};
#endif // !ENUMS_H
